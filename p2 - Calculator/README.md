Prompt used to generate code for calculator app:


Hi Gemini I need your help to generate a command line calculator app and it needs to have the following functionality:

The program must first prompt the user to enter a number.

Next, it must prompt for an arithmetic operation (+, -, *, /).

Finally, it must prompt for a second number.

The program must calculate and display the result in a clear, human-readable format.

Error Handling: The application must not crash if the user enters non-numeric input or attempts to divide by zero. In these error cases, a friendly message should be displayed to the user.


I used the parameters for the assinment thinking that there would be a couple things to change or fix as vibe coding usually results in a couple bugs, but to my surprise the whole program worked off of one prompt. I think that since we gave the AI all the specifications of the project that it was able to follow through fairly quickly and efficiently without any problems.